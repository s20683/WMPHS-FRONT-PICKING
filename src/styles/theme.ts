import { createTheme } from "@mui/material";
export const theme = createTheme({
    palette: {
        secondary: {
            main: "#f7b736",
        },
    },
    typography: {
        body1: {
            color: "#616161",
        },
    },
});

export default theme;